<h2>New text sandbox</h2>

<%= render "form.html", changeset: @changeset,
                        action: text_sandbox_path(@conn, :create) %>

<%= link "Back", to: text_sandbox_path(@conn, :index) %>
