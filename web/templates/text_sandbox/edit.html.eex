<h2>Edit text sandbox</h2>

<%= render "form.html", changeset: @changeset,
                        action: text_sandbox_path(@conn, :update, @text_sandbox) %>

<%= link "Back", to: text_sandbox_path(@conn, :index) %>
